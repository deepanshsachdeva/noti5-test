<!DOCTYPE html>
<html>
  <head>
    <title>User Profile</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/profile.css">
  </head>
  <body>
    <div id='container'>
        <% if(pgData.status == 'error' || pgData.status == 'none'){ %>
          <div class="alert alert-danger" role="alert" style="text-align:center;">
            <strong>Error:</strong> invalid user
          </div>
        <% } else{ %>
          <div class='user-profile'>
            <h1><%= pgData.user %></h1>
            <h3>Email: <a href="mailto:<%= pgData.email %>"><%= pgData.email %></a></h2>
            <h3>No. of posts: <%= pgData.count %></h2>
            <a class="btn btn-default" href="/timeline" role="button">Back to timeline</a>
          </div>
        <% } %>
    </div>
  </body>
</html>
